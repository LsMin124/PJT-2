# AMR 견적 시뮬레이션 서비스 — End-to-End 구현 결정 인벤토리

> 실제 구현 전 결정해야 할 사항의 전수 목록.
> 표기: ★ = 크리티컬 패스 (다른 결정들이 이 결정에 종속됨) / 권고 = 논의 기반 기본값 제안

---

## A. 데이터 모델 / 입력 스키마 (시스템 전체의 계약)

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| A1 ★ | **맵 표현** | 균일 그리드 vs 그래프(노드-엣지) | 그리드 기반이되 내부적으로 그래프로 변환. VDA 5050이 노드/엣지 모델이므로 그래프가 최종 공용어 |
| A2 ★ | 그리드 셀 크기 | 0.5m / 1m / 로봇 풋프린트 기준 | 로봇 풋프린트 ≒ 1셀이 되는 크기(예: 1m). MAPF 문헌 관례와 일치 |
| A3 ★ | **좌표계/단위 단일 소스** | SimPy 그리드 좌표 ↔ Isaac 미터 ↔ 웹 캔버스 픽셀 3중 표현 | 미터 기반 세계 좌표를 원본으로, 나머지는 변환 함수로 파생. 변환 로직을 공유 라이브러리 한 곳에 |
| A4 | 통로 방향성 | 양방향 vs 단방향(one-way) 지정 가능 여부 | 에디터에서 단방향 지정 지원 — 정체 완화의 핵심 설계 변수이자 what-if 재료 |
| A5 | 시맨틱 요소 스키마 | 랙/피킹 스테이션/충전소/대기 구역/입출고 도크의 속성 정의 | 각 요소의 용량(스테이션 동시 처리 수), 접근 셀(approach cell) 명시 |
| A6 | 스키마 버저닝 | 없음 vs semver | `schema_version` 필드 필수. 스키마 변경 시 마이그레이션 비용이 3개 컴포넌트에 전파됨 |
| A7 | 주문 도착 모델 | 정적 시나리오(사전 생성 목록) vs 확률 프로세스(시간대별 포아송/NHPP) | 둘 다: 확률 프로세스로 생성하되 시드 고정으로 재현 가능한 정적 목록화. 공정 비교의 전제 |
| A8 | 주문 구조 | 단일 라인 vs 멀티라인(주문당 여러 SKU) | v1은 단일 반송 작업 단위로 단순화, 멀티라인 피킹 투어는 v1.5 |
| A9 | SKU–랙 매핑 | 개별 SKU 좌표 vs ABC 존 근사 | ABC 존 근사 (기결정). 존별 방문 확률 분포로 표현 |
| A10 | 로봇 파라미터 스키마 | 속도만 vs 가감속·회전·픽드롭 시간 포함 | 최소: 최대속도, 가감속, 회전 시간, 픽/드롭 시간, 배터리 용량/소모율/충전율, 풋프린트 |
| A11 | 배터리 모델 충실도 | 무시 / 선형 방전 / 충전 곡선 | 선형 방전 + 상수 충전율. 충전이 대수 산정에 미치는 영향이 커서 무시 불가, 곡선까지는 과잉 |
| A12 | WMS 로그 파싱 범위 | 자유 형식 vs 고정 템플릿 | 고정 엑셀 템플릿(컬럼 명세 공개) + 파싱 실패 시 폼 입력 폴백 |

## B. SimPy DES 엔진

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| B1 ★ | **시간 모델** | 고정 틱(예: 1초) vs 연속 이벤트 | 고정 틱. MAPF 예약 테이블이 이산 타임스텝 전제라 정합. 틱 크기 = 셀 통과 시간과 일치시키면 "1틱 = 1셀 이동" 단순화 |
| B2 | 이동 모델 | 셀 단위 이벤트(틱마다) vs 구간 단위 이벤트 | 셀 단위 — 예약 테이블·경합·데드락이 자연 창발. 성능 문제 시 무경합 구간만 벌크 스킵 최적화 |
| B3 | 가감속 반영 | 무시(등속) vs 구간 평균 유효속도 vs 프로파일 | 유효속도 보정 계수(에뮬레이션 실측으로 캘리브레이션). 프로파일 시뮬은 DES 목적에 과잉 |
| B4 ★ | 재계획 주기 | 이벤트 트리거(작업 완료 시) vs 고정 윈도우(RHCR 방식 W틱마다) | RHCR 스타일 고정 윈도우 + 작업 발생 이벤트 병행. 윈도우 크기 h는 실험 파라미터 |
| B5 | 데드락 처리 | 발생 불가 설계(예약 보장) vs 탐지 후 재계획 | 예약 테이블 기반이면 원천 차단 가능. 단 "탐지 카운터"는 남겨서 버그 검출용으로 |
| B6 | 확률 분포 선택 | 픽 시간, 주행 변동: 상수 / 정규 / 로그정규 / 삼각 | 픽 시간 로그정규(현장 데이터 관례), 주행은 v1 결정적 + 유효속도 보정. 분포 가정을 리포트에 명시 (VDI 3633) |
| B7 ★ | 워밍업 기간 | 없음 vs 초기 과도상태 절단 | 필수. 빈 창고에서 시작하는 초기 구간은 통계에서 제외. 절단 시점 결정 방법(Welch 그래프 or 고정 30분) 문서화 |
| B8 | 런 길이 / 반복 수 | 고정(8시간 × 20시드) vs CI 폭 기준 적응적 | v1 고정 (8h × 20시드), CI 폭이 목표(예: ±5%) 초과 시 시드 추가하는 룰 명시 |
| B9 | 통계 수집 방식 | 전 이벤트 로그 vs 온라인 집계 | 이중화: 집계 지표는 항상, 상세 이벤트 로그는 플래그로 (리플레이/디버깅용, 용량 문제 → E5) |
| B10 | 성능 목표 | — | 1런(8h, 로봇 10대) < 10초 목표 설정 후 프로파일링. what-if 응답성의 상한 결정 |

## C. 디스패처 / AI

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| C1 | 인터페이스 시그니처 | `assign(pending_tasks, robot_states, map_ctx) -> allocations` | 전략 패턴 고정 (기결정). 반환에 "할당 보류"도 표현 가능해야 함 |
| C2 | 할당 시점 | 즉시(작업 발생 시) vs 배치(주기적 일괄) | 배치 주기와 B4 재계획 윈도우를 동기화 — 헝가리안이 배치 전제이므로 |
| C3 | 헝가리안 비용행렬 | 순수 거리 vs 거리+대기 vs 경합 페널티 포함 | 단계적: v1 거리+대기, v1.5 경합 페널티 (TAPF 절충안) |
| C4 ★ | **RL 일반화 전략** | (a) 맵별 재학습 (b) 맵 불변 관측 설계로 zero-shot (c) RL을 옵션 연구로 축소 | **최대 함정** — 견적 서비스는 고객마다 맵이 다름. 맵 특정 정책이면 서비스 성립 불가. 권고: 관측을 맵 불변 특징(작업까지 경로비용, 로봇 부하, 구역 혼잡도)으로 설계 + 랜덤 레이아웃 학습. 실패 시 (c)로 후퇴하고 리포트에는 휴리스틱을 기본 탑재 |
| C5 | RL 관측/행동 공간 | 가변 로봇/작업 수 문제 | 최대 N 고정 + 마스킹, 또는 (작업,로봇) 쌍 스코어링 방식. 후자가 일반화에 유리 |
| C6 | RL 알고리즘/프레임워크 | PPO(SB3) vs DQN vs RLlib | PPO + Stable-Baselines3. 팀 학습 곡선 최소화 |
| C7 | 학습/평가 분리 | — | 학습용 시드·레이아웃과 평가용 완전 분리. 평가 시나리오는 스윕과 동일 조건 |
| C8 | 보상 설계 | -makespan vs 처리량 vs shaped(대기 페널티 등) | 처리량 기반 + 대기시간 페널티. makespan은 lifelong 설정에 부적합 |

## D. 경로계획 (MAPF 구현)

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| D1 | v1 플래너 | Prioritized Planning + 시공간 예약 테이블 | 기결정. 우선순위 규칙(작업 긴급도? 선착?)만 추가 결정 |
| D2 | 예약 테이블 구조 | dict[(cell, t)] vs 인터벌 기반 | dict 기반으로 시작, 성능 병목 시 개선 |
| D3 | 로봇 점유 모델 | 1로봇 = 1셀 vs 풋프린트 다셀 | 1셀 (A2와 연동). 이기종 크기 로봇은 v2 |
| D4 | 회전 비용 | 무시 vs 회전 시간 반영 | 반영 (틱 단위 근사). 차동구동 AMR은 회전이 실측 시간의 유의미한 비중 |
| D5 | 대기(idle) 로봇 위치 | 현 위치 정차 vs 지정 대기 구역 복귀 | 지정 대기 구역 — 통로 점거 방지. 대기 구역 배치가 what-if 변수가 됨 |
| D6 | 벤치마크 검증 | — | Stern 2019 공개 벤치마크 소규모 인스턴스로 플래너 정합성 확인 (V&V 스토리) |

## E. 백엔드 / 인프라

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| E1 | API 패턴 | 동기 vs 비동기 잡(제출→폴링/푸시) | 비동기: POST /scenarios → jobId → WebSocket 진행률 + GET 결과 |
| E2 ★ | 잡 오케스트레이션 | K8s Job 직접 생성 vs 큐(Redis/Celery류) 경유 vs K8s Indexed Job | Scenario API가 K8s Indexed Job 생성(스윕 1개 = Job 1개, 인덱스 = 시나리오 조합). 별도 큐 시스템 추가는 과잉 |
| E3 | 잡 실패 정책 | 재시도 횟수, 타임아웃, 부분 실패 시 결과 처리 | 재시도 2회, 런 타임아웃 5분, 부분 실패는 결과에 "미완료 조합" 명시 (투명성) |
| E4 | Kafka 메시지 스키마 | JSON vs Avro + Schema Registry | v1 JSON + 스키마 문서화. Avro/Registry는 학습 가치 있으나 팀 부하 고려해 선택 |
| E5 ★ | 결과 저장 정책 | 전 이벤트 로그(런당 수십 MB × 360런) vs 집계만 | 집계 + 시계열 다운샘플 기본, 상세 로그는 "대표 런"(중앙값 시드)만 보존 — 리플레이 시각화용 |
| E6 | DB 스키마 | scenario / sweep / run / metric 계층 설계 | run은 (sweep_id, param_combo, seed) 유니크. 지표는 hypertable |
| E7 | 로컬 개발 환경 | docker-compose vs 로컬 k3d/k3s vs 클라우드 | 개발은 docker-compose, 스윕 데모는 k3d(로컬) — 클라우드 비용 리스크 회피. 발표용 클러스터는 막판 결정 |
| E8 | 멀티테넌시/인증 | 없음 vs 세션 수준 vs 완전 격리 | 데모 수준: 프로젝트(견적 건) 단위 스코프만. 진짜 인증은 범위 외 명시 |
| E9 | 저장소 구조 | 모노레포 vs 멀티레포 | 모노레포 + 스키마/공용 타입을 shared 패키지로 — 계약(A6) 관리가 쉬움 |
| E10 | CI/CD | — | GitLab CI (SSAFY 자산 재사용): 스키마 검증 + 단위 테스트 + 소규모 스모크 시뮬 |

## F. 에뮬레이션 계층 (ROS2 + Isaac Sim)

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| F1 ★ | **경로 지시 방식** | (a) VDA 5050 노드/엣지 경로 하달(중앙 계획) (b) 목적지만 주고 Nav2 자율 경로 | **(a) 필수.** SimPy는 중앙 플래너 세계인데 Isaac에서 Nav2가 제멋대로 경로를 고르면 두 계층이 서로 다른 시스템을 시뮬레이션하는 것 — 정합성 비교가 무의미해짐. VDA 5050 표준 자체가 (a) 모델. Nav2는 노드 간 구간 주행만 담당 |
| F2 | VDA 5050 서브셋 | order/state/connection 필수, instantActions/factsheet/visualization 선택 | 필수 3종 + instantActions(정지/재개, 데모용). 버전은 2.0 기준 명시 |
| F3 | 맵 → USD 변환 | 수동 씬 제작 vs 시맨틱 그리드에서 자동 생성 스크립트 | 자동 생성 (랙=박스 프리미티브 수준). "고객 맵이 곧 검증 환경"이 되는 것 자체가 데모 포인트 |
| F4 | 검증 대상 로봇 | iw.hub 메인 + MiR 250 서브 (기결정) | Nova Carter로 파이프라인 스모크 테스트 먼저 |
| F5 ★ | RTF 실측 | — | 프로젝트 극초반에 RTX 5080 기준 "RTF ≥ 0.9 유지 최대 대수" 실측 → 검증 스코프 확정. 센서(라이다 레이 수) 최소화 설정 포함 |
| F6 | 시간 기준 | sim time vs wall clock, VDA 5050 timestamp | ROS 노드는 use_sim_time 통일, VDA 5050 timestamp는 sim time 사용 + FMS 쪽 타임아웃을 RTF 보정 |
| F7 | 정합성 오차 지표 | 무엇을 비교? | 3단: ①구간(엣지) 주행시간 분포 ②작업 사이클 타임 ③시간당 처리량. ①이 보정 루프의 입력, ③이 리포트 헤드라인 |
| F8 | 보정 루프 절차 | — | 에뮬레이션 실측 엣지 주행시간 → SimPy 유효속도 계수 갱신 → 재스윕. 1회 이상 왕복을 일정에 명시 |
| F9 | 어댑터 | vda5050_connector 계열 오픈소스 이식 (기결정) | 자작 금지. 포크 후 필요 수정만 |

## G. 프론트엔드

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| G1 | 스택 | React + 캔버스 라이브러리(Konva/SVG/deck.gl) | React + Konva (2D 탑뷰 기결정). deck.gl은 과잉 |
| G2 | 에디터 검증 | 저장 시 검증 vs 실시간 | 실시간: 연결성(모든 스테이션 도달 가능), 최소 통로 폭, 충전소 개수 경고. 잘못된 맵이 시뮬 오류로 늦게 발견되는 것 방지 |
| G3 | 결과 화면 우선순위 | 곡선/히트맵/리플레이 | ①대수–처리량–TCO 곡선(선택 UI) ②병목 히트맵 ③대표 런 리플레이 애니메이션 — 이 순서로 구현 |
| G4 | 리포트 출력 | 웹 뷰 vs PDF 생성 | 웹 뷰 우선, PDF는 인쇄 CSS로 저비용 대응. 전용 PDF 생성은 여유 시 |
| G5 | 진행률 UX | 스피너 vs 스윕 매트릭스 실시간 채움 | 스윕 조합별 완료 상태가 채워지는 매트릭스 — "수백 개 시뮬이 병렬로 도는 중"이 보이는 것 자체가 임팩트 |

## H. 실험 설계 / 통계

| # | 결정 항목 | 선택지 / 쟁점 | 권고 |
|---|---|---|---|
| H1 | 스윕 격자 | 대수 범위 자동 결정 방법 | 하한: 총 수요/로봇 처리능력 추정치, 상한: 포화 감지(증분 <2% 시 중단)로 적응적 확장 |
| H2 | 신뢰구간 | t-분포 기반 95% CI (시드 간) | 기결정. 리포트에 CI 항상 병기 |
| H3 | 민감도 분석 | OAT(one-at-a-time) vs 전역(Sobol) | v1 OAT: 픽 시간 ±20%, 수요 ±30%, 유효속도 ±10%에 대한 최적 대수 안정성. Sobol은 과잉 |
| H4 | 디스패처 비교 프로토콜 | — | 동일 시드 쌍대 비교(paired) — 분산 줄고 통계 검정 명확 |
| H5 | 최적점 선정 규칙 | 처리량 목표 충족 최소 대수 vs ROI 최대점 | 둘 다 제시하고 선택은 고객에게 — "선택하게 하는" 제품 철학과 일치 |

## I. 검증 (V&V — VDI 3633 대응)

| # | 결정 항목 | 내용 |
|---|---|---|
| I1 | Verification | 보존 법칙 체크(생성 주문 수 = 완료+미완료), Little's Law 검산(L=λW), 로봇 1대 극한 케이스의 해석해 대조 |
| I2 | Validation | MAPF 공개 벤치마크 소규모 대조(D6), SimPy vs 에뮬레이션 정합성(F7-8), 문헌 처리량 수치와의 자릿수 비교 |
| I3 | 문서화 | 모든 가정·분포·보정 계수를 리포트 부록에 — "가정 명세서"가 투명성 주장의 실체 |

## J. 프로젝트 운영

| # | 결정 항목 | 권고 |
|---|---|---|
| J1 | 인터페이스 경계 | 팀 간 계약 = ①입력 JSON 스키마 ②Kafka 이벤트 스키마 ③디스패처 인터페이스 ④VDA 5050 서브셋. 이 4개를 1주차에 동결 |
| J2 | 데모 폴백 | 라이브 실패 대비: 스윕은 사전 실행 결과 캐시, 에뮬레이션은 녹화 영상 준비. what-if만은 라이브 사수 |
| J3 | 결정 기록 | 본 문서의 각 결정을 ADR(Architecture Decision Record) 경량 포맷으로 저장소에 — 면접에서 "왜"를 답하는 원천 |

---

## 크리티컬 패스 요약 (1~2주차에 반드시 확정)

1. **A1–A3 맵 표현·셀 크기·좌표계** — 모든 컴포넌트가 종속
2. **B1 시간 모델(고정 틱)** — MAPF 통합·재계획·통계의 전제
3. **F1 경로 지시 방식(중앙 하달)** — 두 시뮬레이션 계층의 정합성 성립 조건
4. **C4 RL 일반화 전략** — 실패 가능성이 있는 유일한 연구 요소, 후퇴선 사전 합의
5. **E2/E5 잡 오케스트레이션·저장 정책** — 인프라 골격
6. **F5 RTF 실측** — 검증 계층의 물리적 상한 확정
